mode: [ cdn ]
cellbang:
  workspace:
    root: /code/template.theia-workspace
  env:
    homeDirUri: /
malagu:
  security:
    enabled: false
    loginRequired: false
  faas-adapter:
    function:
      codeUri:
        include: templates
frontend:
  malagu:
    security:
      logoutSuccessUrl: '{redirect}'
