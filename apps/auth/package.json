{
  "name": "auth",
  "description": "Auth",
  "keywords": [
    "malagu-component"
  ],
  "version": "0.0.0",
  "license": "MIT",
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@cellbang/accounts-client": "0.0.0",
    "@cellbang/config": "0.0.0"
  },
  "devDependencies": {
    "@malagu/cli": "^1.13.1",
    "rimraf": "^2.6.3",
    "@types/react": "^16.9.38",
    "@types/react-dom": "^16.8.5",
    "@types/react-router": "^5.1.7",
    "@types/react-router-dom": "^5.1.5"
  },
  "scripts": {
    "clean": "rimraf lib dist .malagu",
    "build": "malagu build",
    "start": "malagu serve",
    "deploy": "malagu deploy -m test",
    "deploy:test": "malagu deploy -m test",
    "deploy:pre": "malagu deploy -m pre",
    "deploy:release": "malagu deploy -m release"
  }
}
