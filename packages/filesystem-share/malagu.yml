mode: [ locales ]
cellbang:
  filesystem:
    share:
      checkShareStatusUrl: /checkShareStatus
      checkShareStatusMethod: POST
malagu:
  cache:
    config:
      filesystem-share:
        store: memory
        max: 100
        ttl: 5 # seconds