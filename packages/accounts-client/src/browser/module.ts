import '../../src/browser/style/index.css';

import { autoBind } from '@malagu/core';
import '.';
export default autoBind((bind, unbind, isBound, rebind) => {
    // rebind(SidebarBottomMenuWidget).to(SidebarRightMenuWidget);
});
