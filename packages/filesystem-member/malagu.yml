mode: [ locales ]
cellbang:
  filesystem:
    member:
      getMemberStatusUrl: /getMemberStatus
      getMemberStatusMethod: POST
malagu:
  cache:
    config:
      filesystem-member:
        store: memory
        max: 100
        ttl: 5 # seconds